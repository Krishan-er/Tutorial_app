<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Manifest_file">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/sw_manifest">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp"
            android:id="@+id/ll_manifest">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/manifest_heading"
                android:text="MANIFESTS"
                android:textSize="50sp"
                android:textStyle="bold"
                android:layout_gravity="center"/>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/mtv1"
                android:text="Every app project must have an AndroidManifest.xml file (with precisely that name) at the root of the project source set. The manifest file describes essential information about your app to the Android build tools, the Android operating system, and Google Play"
                android:textSize="22sp"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/mtv2"
                android:layout_margin="12dp"
                android:text="\nIt is a declaration file.
\n \nHere only Which activity should start first, that has been declared.
\n \nIt declares which permissions the application must have.
\n \nIt also declares the permissions that others are required to have in order to interact using intent filter.
\n \nIntent filter shows which activity will appear at very first
\n \nIt declares the minimum level of the Android API.
\n \nIt lists the libraries that the application must be linked.
\n \nAll the component should declared here.
\n \nThe components are activities, services, broadcast receivers, and content providers.
\n \nThis is how the Manifest file looks like "
                android:textSize="22sp"
                />
            <ImageView
                android:layout_width="match_parent"
                android:layout_height="310dp"
                android:id="@+id/manifest_img"
                android:src="@drawable/manifest_file"/>
        </LinearLayout>
    </ScrollView>
</android.support.v7.widget.LinearLayoutCompat>