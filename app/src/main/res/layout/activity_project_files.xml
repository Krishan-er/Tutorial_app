<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Project_files">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/sw_p_Struct">
        <LinearLayout
            android:layout_width="match_parent"
            android:id="@+id/ll_structure"
            android:padding="20dp"
            android:orientation="vertical"
            android:layout_height="wrap_content">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/pstv1"
                android:text="PROJECT STRUCTURE"
                android:textSize="50sp"
                android:textStyle="bold"
                android:gravity="center"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/pstv2"
                android:text="Android Project Files"
                android:layout_marginTop="12dp"
                android:textSize="25sp"
                android:textStyle="bold"/>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/pstv3"
                android:text="By default, Android Studio displays your project files in the Android view. This view does not reflect the actual file hierarchy on disk, but is organized by modules and file types to simplify navigation between key source files of your project, hiding certain files or directories that are not commonly used. Some of the structural changes compared to the structure on disk include the following:"
                android:layout_marginTop="12dp"
                android:textSize="22sp" />
            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="200dp"
                android:id="@+id/img1"
                android:layout_gravity="center"
                android:src="@drawable/diff_structs"
                />
            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="450dp"
                android:id="@+id/img2"
                android:layout_gravity="center"
                android:src="@drawable/proj_struct"
                />
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/pstv4"
                android:text="* Shows all manifest files for each module in a module-level group (when you have different manifest files for different product flavors and build types).
\n \n* Shows all the java files which contain the what all actions is to be perform behind the UI widgets
\n \n* Shows all alternative resource files in a single group, instead of in separate folders per resource qualifier,like all images resource is drawable \n Layout is for storing XML files \n Values contain all design files like color,styles for better UI.
\n \n* Shows all the project's build-related configuration files in a top-level Gradle Script group.
"
                android:layout_marginTop="12dp"
                android:textSize="22sp" />
        </LinearLayout>

    </ScrollView>

</android.support.v7.widget.LinearLayoutCompat>